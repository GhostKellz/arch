# Optional bind override for Uptime Kuma. Copy to .env next to docker-compose.yml.
# Leave as loopback if nginx terminates on the same host; set to the tailnet IP if
# the reverse proxy is remote.
KUMA_BIND_IP=127.0.0.1
